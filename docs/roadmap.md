# Roadmap

This is the canonical living feature tracker for the repository.

Use this file when you want the current state of the project in one place:

- what major feature areas exist
- what is implemented vs planned
- which document is authoritative for details
- what should happen next

## How This Relates To Other Docs

- `plan-memoryGraphFlaskApi.prompt.md` is the original feature/spec prompt.
- `docs/vision.md` is the overall system vision, including Claude Code's role as the runtime.
- `docs/architecture.md` is the current architecture narrative and system-boundary document.
- `docs/deep-dive/README.md` is the subsystem-level implementation-state companion to the architecture doc.
- `docs/adr/README.md` is the index of durable architecture decision records.
- `docs/phase1-2-consolidated.md` is the retrospective summary of early review and implementation work.
- `docs/phase3-consolidated.md` is the consolidated retrospective summary of Phase 3 implementation, rationale, and decisions.
- `docs/plans/README.md` defines the plan document lifecycle and active-plan usage.
- `docs/conversation-outcomes.md` records major discussion outcomes and rationale.

If those documents disagree, treat current code and `README.md` as authoritative,
then update this roadmap and the stale doc.

## Status Legend

- Implemented
- In progress
- Planned
- Deferred

## Feature Matrix

| Feature Area                                   | Status               | Scope Summary                                                                                                                                                    | Primary Source of Truth                                                                              | Implemented Surface                                                                                                                                                                                                                                  | Next Step                                                                                                                             |
| ---------------------------------------------- | -------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------- |
| Core Flask service foundation                  | Implemented          | App factory, SQLite init, blueprints, JSON error handling, CORS, local-first runtime                                                                             | `README.md`, `docs/phase1-2-consolidated.md`                                                         | `api_server.py`, `config.py`, `db_schema.py`, `blueprints/*`                                                                                                                                                                                         | Maintain as baseline; no structural gap                                                                                               |
| Conversations                                  | Implemented          | Log, recent retrieval, FTS search, stats                                                                                                                         | `README.md`, `docs/phase1-2-consolidated.md`                                                         | `/conversation/log`, `/conversation/recent`, `/conversation/search`, `/conversation/stats`                                                                                                                                                           | Add correlation IDs if ops tracing becomes necessary                                                                                  |
| Memory CRUD and scoped access                  | Implemented          | Memory create/list/search/recall/delete with shared/private scope semantics                                                                                      | `README.md`, `docs/deep-dive/lifecycle-and-trust.md`                                                 | `/memory`, `/memory/list`, `/memory/recall`, `/memory/search`, `/memory/<id>`, `/memory/<id>/promote`                                                                                                                                                | Keep docs and tests aligned with current API                                                                                          |
| Memory lifecycle operations                    | Implemented          | Archive, invalidate, merge, supersede, promote, verification status updates                                                                                      | `README.md`, `docs/deep-dive/lifecycle-and-trust.md`                                                 | `/memory/archive`, `/memory/invalidate`, `/memory/merge`, `/memory/supersede`, `/memory/verify`                                                                                                                                                      | Keep the current model; extend only if a concrete operational gap emerges                                                             |
| Autonomous-session write discipline            | Implemented          | Batch write, idempotency, run tracking, tags, restart-safe checkpoint conventions                                                                                | `docs/agent-memory-ops.md`, `.github/copilot-instructions.md`                                        | `/memory/batch`, `idempotency_key`, `run_id`, `tags`                                                                                                                                                                                                 | Increase real usage so usefulness metrics become informative                                                                          |
| Retrieval quality controls                     | Implemented          | Visibility/owner/status filters plus `run_id`, `tag`, `min_confidence`, `updated_since`, `recency_half_life_hours`, and typed metadata filters                   | `README.md`, `docs/deep-dive/retrieval-contracts.md`                                                 | memory list/search/recall query params                                                                                                                                                                                                               | Monitor ranking quality as filter combinations expand                                                                                 |
| Explicit retrieval profiles                    | Implemented          | `profile=general\|autonomous` with distinct defaults; autonomous mode requires `agent_id` and applies stricter confidence and recency defaults                   | `README.md`, `docs/deep-dive/retrieval-contracts.md`                                                 | `profile` param on `/memory/list`, `/memory/recall`, `/memory/search`                                                                                                                                                                               | Keep stable; extend to additional read surfaces only if the autonomous agent demonstrates a concrete gap                              |
| Semantic and hybrid search                     | Implemented          | Embedding-backed semantic search plus hybrid RRF retrieval                                                                                                       | `README.md`, `docs/phase1-2-consolidated.md`                                                         | `/search/semantic`, `/search/hybrid`, `/embeddings/stats`, `/embeddings/reindex`                                                                                                                                                                     | Consider async enrichment if request-path latency becomes a problem                                                                   |
| Embedding correctness and dedup                | Implemented          | Reindex updates FKs, dedups duplicate text, repairs legacy rows, unique text index                                                                               | `docs/phase1-2-consolidated.md`                                                                      | `db_schema.py`, `storage/embedding_repository.py`, `/embeddings/reindex`                                                                                                                                                                             | Monitor only; no urgent gap                                                                                                           |
| Entity and KV support                          | Implemented          | Basic entity insert/search and KV reads/writes                                                                                                                   | `README.md`                                                                                          | `/entity`, `/entity/search`, `/kv/<key>`                                                                                                                                                                                                             | Expand only if the autonomous agent or a general-agent client demonstrates a concrete need                                            |
| Web UI                                         | Implemented baseline | D3-based local UI for graph/log/rag/crons navigation                                                                                                             | `README.md`, `docs/phase1-2-consolidated.md`                                                         | `/graph`, `static/index.html`                                                                                                                                                                                                                        | Add Brain tab (consolidated audit surface for goals, plans, memory layers, autonomy gates) once bridge primitives are stable           |
| Documentation hub and outcomes tracking        | Implemented          | README as doc hub, outcomes ledger, roadmap, consolidated summaries                                                                                              | `README.md`, `docs/conversation-outcomes.md`, `docs/roadmap.md`                                      | repo docs                                                                                                                                                                                                                                            | Keep this roadmap current when features shift                                                                                         |
| Architecture narrative and ADRs                | Implemented          | Current-state architecture document, vision document, and ADRs for durable system-boundary decisions                                                             | `docs/architecture.md`, `docs/vision.md`, `docs/adr/README.md`                                       | architecture overview, vision doc, ADR index and records                                                                                                                                                                                             | Add new ADRs only for durable structural decisions                                                                                    |
| Operational adoption of memory signals         | Implemented          | Drive consistent real-world use of `run_id`, `idempotency_key`, `tags`, and verification updates so memory usefulness metrics reflect actual workflow behavior   | `docs/agent-memory-ops.md`, `docs/deep-dive/operations-and-maintenance.md`, `README.md`              | Sprint A complete (2026-04-21): signal patterns documented, worked examples in copilot-instructions and README, and scorecard adoption coverage in tests                                                                                             | Completed; keep signal usage consistent in normal workflows and monitor scorecard drift over time                                      |
| Memory usefulness observability                | Implemented          | Current-state counts, adoption/trust coverage, run-level signals, and freshness signals                                                                          | `README.md`, `docs/deep-dive/operations-and-maintenance.md`                                          | `/metrics/memory-usefulness`                                                                                                                                                                                                                         | Keep scorecards lightweight; add persisted history only if the autonomous agent demonstrates a concrete operational gap                |
| Request correlation and broader ops visibility | Implemented          | Request IDs in response headers plus correlation IDs in global error payloads and request logging; route-level request/error/latency counters via `/metrics/ops` | `README.md`, `docs/deep-dive/operations-and-maintenance.md`                                          | global `X-Request-Id` propagation, request-id-aware global error responses, `/metrics/ops` per-route counters                                                                                                                                        | Keep counters ephemeral unless real operations prove restart-persistent history is necessary                                           |
| Structured search error semantics              | Implemented          | Remove broad exception swallowing in retrieval endpoints so failures propagate to the global 500 handler and return observable error responses                   | `blueprints/conversations.py`, `blueprints/memory.py`                                                | bare propagation to Flask global error handler on all FTS and search paths                                                                                                                                                                           | Done; revisit if per-error-type handling becomes necessary                                                                            |
| Service-layer domain boundaries                | Implemented          | Blueprints are thin HTTP adapters while repositories and services own SQL, lifecycle, write, retrieval, and hybrid-ranking concerns                              | `docs/architecture.md`                                                                               | `blueprints/_params.py`, `storage/*`, `services/*`, thin adapters in `blueprints/*`                                                                                                                                                                  | Keep new boundaries enforced; add targeted unit tests around service modules                                                          |
| Stale private memory cleanup                   | Implemented          | Cleanup command/job with dry-run and summary                                                                                                                     | `docs/deep-dive/lifecycle-and-trust.md`, `docs/deep-dive/operations-and-maintenance.md`, `README.md` | Sprint B complete (2026-04-21): `POST /memory/cleanup-private` with retention-days, dry-run mode, owner/status filters, and deletion summary                                                                                                         | Completed; add follow-ons only with concrete operational value                                                                        |
| Operational maintenance follow-ons             | Implemented          | Integrity checks, SQLite maintenance helper, and deeper operational signals                                                                                      | `docs/deep-dive/operations-and-maintenance.md`, `README.md`                                          | Sprint C complete (2026-04-21): `GET /maintenance/integrity`, `POST /maintenance/sqlite`, and extended `GET /metrics/ops` signals                                                                                                                    | Completed; Phase 3 done                                                                                                               |
| Typed metadata filtering                       | Implemented          | JSON metadata write support, server-side typed metadata filtering, and parsed metadata response fields across list/search/recall                                 | `README.md`, `docs/deep-dive/retrieval-contracts.md`, `tests/test_memory.py`                         | `metadata_json` persistence + `metadata_key`/`metadata_value`/`metadata_value_type` filters + `metadata` parsed response field                                                                                                                       | Extend to hybrid/semantic search only if required                                                                                     |
| Bridge primitives (M1-M3)                      | In progress          | Durable records for the autonomous agent: goals, action logs, and autonomy checkpoints with owner/run scoping, idempotency replay, and auditable gate decisions  | `docs/plans/harness-v2-bridge-primitives.md`, `README.md`                                            | `goals` + `goal_status_history` + `action_logs` + `autonomy_checkpoints` schema, `/goal`, `/goal/<id>`, `/goal/list`, `/goal/<id>/status`, `/action-log`, `/action-log/list`, `/action-log/<id>/complete`, `/autonomy/check`, `/autonomy/check/list` | M4: docs deep-dive promotion, response shape fixes (constraints_json deserialization, same-status transition guard), integration polish |
| Safety tier surfaces                           | Planned              | Plan nodes (executable goal decompositions with status tracking), sandbox records (dry-run gate for irreversible actions with verdict), and verification records (claim assessments with sources and confidence score) | `docs/vision.md`                                                                                     | not implemented                                                                                                                                                                                                                                      | Implement after bridge primitives M4; plan nodes first as they are most directly load-bearing for session recovery                    |
| Self-improvement tier surfaces                 | Planned              | Capability records (Bayesian confidence per skill), world model predictions (testable claims + calibration), experiments (approach A/B with auto-conclude), metric write path (daily KPIs), skill records (maturity lifecycle), memory decay (weekly confidence reduction on unverified memories) | `docs/vision.md`                                                                                     | not implemented                                                                                                                                                                                                                                      | Implement after safety tier; defer individual surfaces until the autonomous agent demonstrates concrete need for each                  |
| Self-evolving surfaces                         | Planned              | Reflections (pattern/mistake/insight summaries), preferences (inferred behavioral rules with confidence gate), insights (typed observations from reflection and world model), proposals (agent-generated code/config changes pending user approval) | `docs/vision.md`                                                                                     | not implemented                                                                                                                                                                                                                                      | Implement alongside or after self-improvement tier; proposals are the write path for preference learning and self-modification         |
| Soft and structured world model                | Planned              | Soft observations (`/worldmodel`) with occurrence boosting and graduation flow into structured `wm_entities`, `wm_relations`, `wm_events`, and `wm_predictions`; three-layer memory read views (`/memory/episodic`, `/memory/semantic`, `/memory/procedural`) | `docs/vision.md`                                                                                     | not implemented                                                                                                                                                                                                                                      | Implement after self-evolving surfaces; world model grower cron depends on `/worldmodel` write path                                   |
| Cron snapshot and autonomy levels              | Planned              | `/cron/active` (POST/GET runtime cron snapshot for dashboard), `/cron/prompts` (parse persisted cron prompt file), `/autonomy/levels` (queryable 6-rung L0–L5 ladder) | `docs/vision.md`                                                                                     | not implemented                                                                                                                                                                                                                                      | Implement with or before self-improvement tier; cron snapshot needed by Crons dashboard tab and bootstrap reconciliation               |
| Backup and restore                             | Planned              | `/backup/info`, `/backup/export` (VACUUM INTO snapshot + SQL dump), `/backup/import` (validate + pre-import backup + hot swap); importance keyword management (`/keywords`) | `docs/vision.md`                                                                                     | not implemented                                                                                                                                                                                                                                      | Implement early as infrastructure; importance keywords needed by conversation log importance scoring                                   |

## Phase 3 Priorities (Current)

1. Keep the implemented API, architecture docs, and ADRs synchronized.
2. Sprint A complete (2026-04-21).
3. Sprint B complete (2026-04-21): stale private memory cleanup.
4. Sprint C complete (2026-04-21): operational maintenance follow-ons.
5. Phase 3 complete; bridge primitives work is post-Phase-3 scope.

## Future Planned Work

Transactional write guarantees are **implemented** (2026-04-21). `write_transaction` context manager
added to `db_utils.py`; batch orchestration and lifecycle service both use service-owned
transactions. Durable behavior documented in `docs/deep-dive/write-atomicity.md`.

Explicit retrieval profiles are **implemented** (2026-04-22). `profile=general|autonomous` supported
on `/memory/list`, `/memory/recall`, and `/memory/search`; autonomous defaults applied when
omitted (`status=active`, `min_confidence=0.7`, `recency_half_life_hours=168`). Documented in
`docs/deep-dive/retrieval-contracts.md`.

Bridge primitives (M1-M3) are **in progress** (2026-04-22). Goals, action logs, and autonomy
checkpoints are implemented in the `plan/harness-bridge-primitives-detail` worktree and pending
merge. M4 polish (response shape fixes, deep-dive docs, integration hardening) remains.

Recommended next steps in priority order:

1. ~~Transactional write guarantees~~ **Implemented** (2026-04-21)
2. ~~Explicit retrieval profiles~~ **Implemented** (2026-04-22)
3. ~~Bridge primitives M1-M3~~ **In progress** — merge after M4 fixes; see `docs/plans/harness-v2-bridge-primitives.md`
4. Session recovery validation — verify the bootstrap flow (snapshot recall + cron reconciliation) works end-to-end with a real autonomous session

Additional future work, only when justified by concrete operational need:

- richer verifier evidence model (explicit evidence records beyond status/source)
- targeted owner-check hardening on mutating routes where caller-supplied ownership is insufficient
- SQLite runtime hardening (connection pragmas and local-concurrency safeguards)
- persisted ops history or task-run trend storage if in-memory metrics stop being sufficient
- memory request model parser cleanup: deduplicate repeated validation-error mapping in `services/memory_request_models.py`

Future work should be planned using feature-specific implementation plans rather than consolidated phase documents.

## Maintenance Rules

1. When a feature changes status, update this file in the same change set as the code or documentation change that caused it.
2. For post-Phase-3 features, create a feature-specific implementation plan in `docs/plans/`.
3. After implementation, promote durable subsystem details from the plan into `docs/deep-dive/` and/or `docs/adr/`; then remove the completed plan from `docs/plans/`.
